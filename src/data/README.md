<!-- Folder purpose: data stores typed content used to render sections without hardcoding text in components. -->

# src/data

Edit `siteData.ts` to change portfolio copy, links, projects, and experience.
